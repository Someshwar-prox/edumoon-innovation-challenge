import { ChatWidgetDemo } from "@/components/widget-demo/chat-widget-demo";

export default function WidgetDemoPage() {
  return <ChatWidgetDemo />;
}

export const metadata = {
  title: "Chat Widget Demo",
  description: "See the AIBridge embeddable chat widget in action.",
};
