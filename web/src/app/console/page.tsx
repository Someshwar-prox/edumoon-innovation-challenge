import { ConsoleShell } from "@/components/console/console-shell";

export default function ConsolePage() {
  return <ConsoleShell />;
}

export const metadata = {
  title: "Console",
  description: "Drive every AIBridge endpoint from your browser.",
};
